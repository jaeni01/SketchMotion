# SketchMotion v2.0 계획서 — 4인 팀 · 실물 하드웨어 통합

> 작성 2026-07-06 · v1.0(시뮬레이션 완결) 기반 · Jira Epic: KAN-16 (v2)
> 설계 상세는 [DESIGN_V2.md](DESIGN_V2.md) 참조.

## 1. 목표 (한 문장)

**"캔버스에 그린 것을, 카메라가 지켜보는 가운데, 실물 로봇팔이 종이에 그리고 실물 AGV가 바닥에 주행한다."**

v1.0의 파이프라인(드로잉→비전→모션)은 그대로 두고, 출력단을 시뮬레이션에서 실물로 교체한다.
핵심 기술 격상: **개루프 → 비전 폐루프(vision-in-the-loop)**. 카메라가 로봇/AGV의 실제 상태를 추정(칼만 필터)하고, 그 추정으로 제어를 보정한다. 이것이 v1과 v2를 가르는 이력서 문장이다:
- v1: "그림을 G-code로 바꿨다"
- v2: **"90fps 글로벌 셔터 카메라로 로봇의 실제 위치를 EKF로 추정하고, 명령 대비 오차를 실시간 보정·리포트하는 폐루프를 만들었다"**

## 2. 보유 장비와 역할

| 장비 | 역할 | 비고 |
|---|---|---|
| SVPRO 2MP 90fps 글로벌 셔터 (AR0234) | **천장(오버헤드) 비전** — AGV 추적 + 로봇 작업 검증 | 글로벌 셔터 → 고속 이동체에 왜곡 없음. 90fps → 추정 루프 주기 11ms. UVC라 기존 Media Foundation 캡처 코드 재사용 |
| myCobot 280 Pi | **실물 드로잉 로봇팔** (6-DOF) | 내장 RPi에서 브리지 서버 실행, PC가 TCP로 경로 전송 |
| AGV | **바닥 경로 주행** — 그린 경로를 따라 주행 | 차동구동 가정(§9 확인 필요), 상부에 마커 부착 |
| STM32 | **AGV 저수준 제어기** — 모터 PWM, 엔코더, α-β 필터 | 하드 리얼타임 담당. UART로 RPi/PC와 통신 |
| Raspberry Pi (별도) | AGV 상위 제어 / 네트워크 브리지 | myCobot 내장 RPi와 별개 |

## 3. 팀 구성 (4인)

| 역할 | 담당 영역 | 주요 산출물 | 필요 스킬 |
|---|---|---|---|
| **A. HMI/통합** (리드) | MFC 앱, Hardware 패널, 오케스트레이션, 최종 데모 | Hardware Pane, 실시간 오버레이 뷰, 데모 시나리오 | MFC/C++, v1 코드베이스 |
| **B. Vision/추정** | AR0234 캡처, 캘리브레이션, 마커 검출, EKF | SM-Tag 검출기, KalmanFilter(Core), 정확도 리포트 | 영상처리, 선형대수, 칼만 |
| **C. 모션/로보틱스** | myCobot 브리지, 펜 홀더, AGV 제어, STM32 펌웨어 | 드로잉 실행기, pure pursuit, 모터 제어 펌웨어 | Python, C(임베디드), 기구 |
| **D. 플랫폼/인프라** | 통신 프로토콜, CI, 통합 테스트, 안전 | 프로토콜 스펙 v1, CI 파이프라인, E-stop 절차 | 네트워크, 빌드/CI, 문서화 |

운영 규칙 (v1에서 확립한 것 유지 + 팀 규칙 추가):
- Jira KAN 보드 = 단일 소스 오브 트루스. 스프린트 2주. 커밋에 `KAN-XX:` 필수.
- **인터페이스 우선**: 사람 경계 = 모듈 경계. 스프린트 1에서 프로토콜/좌표계 스펙을 확정하고, 이후 변경은 Confluence 결정 기록(ADR) + 전원 합의로만.
- PR 리뷰 1인 이상 필수. `main`은 항상 빌드+테스트 그린. 브랜치명 `feature/KAN-XX-설명`.
- 매 스프린트 말 **통합 리허설**(전 모듈 연결 데모) — 마지막에 몰아서 통합하다 터지는 것 방지.

## 4. 마일스톤 (스프린트 5개, 10주)

### Sprint 1 (주 1–2): 계약과 기반
- [D] 프로토콜 스펙 v1 확정: PathML(JSON), 브리지 명령셋, 좌표계 정의 문서
- [B] AR0234 → 기존 CameraPane 연결 확인(UVC/MF), 카메라 내부 파라미터 캘리브레이션
- [C] myCobot 브리지 서버 스켈레톤(RPi, pymycobot), 수동 조그 확인 / STM32 개발환경
- [A] Hardware Pane 골격(연결 상태, E-stop 버튼), v1 코드 인수인계 세션
- **게이트**: PC에서 "HELLO" → myCobot이 홈 포즈로 이동

### Sprint 2 (주 3–4): 로봇팔이 그린다 (개루프)
- [C] 경로 실행기: PathML → 관절 궤적 → 실물 드로잉. **스프링 펜 홀더** 제작·장착
- [A] 캔버스 → "Draw on Robot" 명령 흐름, 진행률 표시
- [B] SM-Tag 마커 검출기 v1 (정지 이미지에서 4×4 비트 태그 검출·디코드)
- [D] CI 구축 (빌드 + Tests 자동), 통합 리허설 #1
- **게이트**: 캔버스에 그린 별 모양을 myCobot이 종이에 그린다 (품질 무관, 완주가 기준)

### Sprint 3 (주 5–6): 카메라가 본다 (추정)
- [B] 실시간 마커 추적(90fps) + **EKF 상태추정** + NIS/혁신 백색성 검증 리포트
- [B] 바닥/종이 평면 호모그래피 캘리브레이션 (4점 마커)
- [A] 카메라 오버레이 뷰: 추정 자세·궤적을 영상 위에 그리기
- [C] AGV 조립 완료, STM32 모터 제어(PWM+엔코더+α-β 필터), 수동 주행
- **게이트**: 손으로 마커를 움직여도 EKF 추정 궤적이 NIS 검정 통과

### Sprint 4 (주 7–8): 폐루프
- [C]+[B] AGV 경로추종: 캔버스 경로 → 바닥 좌표 → pure pursuit, EKF 추정을 피드백으로
- [B]+[A] 로봇팔 검증 리포트: 펜 끝 마커 추적 → 명령 궤적 vs 실제 궤적 오차 맵
- [D] 지연 측정(카메라→PC→WiFi→AGV) 및 KF 예측 보상 적용
- **게이트**: 캔버스에 그린 'ㄹ'자 경로를 AGV가 바닥에서 추종 (RMS 오차 목표 < 5cm)

### Sprint 5 (주 9–10): 통합·완성
- [전원] 풀 데모: 그리기 → 로봇팔 실물 드로잉 + AGV 주행 동시, 원버튼 시나리오
- [A] UI 마감, [B] 정확도 리포트 자동화, [C] 안전(속도 제한·소프트리밋), [D] 데모 영상·문서
- **게이트(v2.0 DoD)**: ① 2막 데모(팔→AGV, 카메라 재배치 포함) 성공률 5회 중 4회 ② 로봇팔 드로잉 평균 오차 **≤2mm 목표 / ≤3mm 합격선** (클로즈업 뷰 0.2mm/px에서 측정 — 근거: 반복정밀도 ±0.5mm는 공식이나 절대정확도는 미공표라 캘리브레이션 후 실측으로 확정) ③ AGV RMS < 5cm ④ EKF NIS 95% 구간 통과 ⑤ 전 과정 데모 영상

## 5. 이론 자료 반영 방침 (제공 문서 4건)

| 문서 | 반영 수준 | 구체적 적용 지점 |
|---|---|---|
| **칼만/상태추정 지식체계** | ★★★ 전면 반영 | AGV EKF(§DESIGN 7), 펜 끝 추적, NIS/혁신 검증을 DoD에 포함, STM32 α-β 필터, 지연 보상(예측), Kabsch/SVD 좌표 정합 |
| **하이브리드 구동 심층정리** | ★★ 개념 차용 | ① **주파수 영역 역할 분담** → 제어 계층 설계 원칙(90Hz 추정 / 20Hz 명령 / 1kHz PWM) ② **순응 요소(SEA) 개념** → 스프링 펜 홀더(Z축 오차를 기계적 순응으로 흡수) ③ EMA/데드존 명령 필터링 |
| **하이브리드 정량 서베이** | ★ 참고 | 액추에이터 선정 근거 서술에 인용 (v2는 전자기 모터 단일 도메인이 옳다는 근거) |
| **포트-해밀토니안 리서치** | ☆ 보류 (v3 연구트랙) | v2 범위에는 부적합(소프트 액추에이터 없음). "에너지 기반 안전 모니터" 아이디어만 백로그에 기록 |

솔직한 판단: PH/소프트 액추에이터 문서는 대학원 연구 방향이지 10주 실물 통합 프로젝트의 재료가 아니다. 다만 **"스프링 펜 홀더 = 하드-소프트 계면에 순응 요소를 끼우는 설계"**라는 연결은 실물로 구현되는 진짜 접점이므로 설계서에 명시했다 — 면접에서 이론과 실물을 잇는 좋은 대화 소재가 된다.

## 6. 추가로 필요한 것 (구매/제작 권장)

| 항목 | 용도 | 긴급도 | 비고 |
|---|---|---|---|
| **스프링 장착 펜 홀더** (myCobot용) | Z축 오차 흡수(순응 요소), 종이 손상 방지 | ★★★ Sprint 2 전 | 3D 프린트 자작 권장(설계 §6.2), 시판품도 있음 |
| **카메라 오버헤드 마운트** (삼각대+횡바 or 천장 브래킷) | AGV 아레나/작업면 수직 뷰 | ★★★ Sprint 1 | 흔들림 없어야 캘리브레이션 유지 |
| **균일 조명** (LED 패널 1–2개) | 마커 검출 안정화(그림자·반사 억제) | ★★☆ Sprint 3 전 | 무광 확산광 |
| **캘리브레이션 보드 인쇄물** (체커보드 A3, 폼보드 배접) | 카메라 내부 파라미터 | ★★★ Sprint 1 | 평평함이 생명 |
| **SM-Tag 마커 인쇄물** (AGV 상판용 대형 1, 바닥 기준 4, 펜 끝 소형 1) | 자세 추정 | ★★★ Sprint 2–3 | 무광 용지 |
| **AGV 주행 매트** (무광 어두운 바닥재, 2×2m 내외) | 대비 확보, 바닥 반사 제거 | ★★☆ | 행사용 카펫/고무매트면 충분 |
| **비상정지 수단** (스위치 달린 멀티탭 최소) | 안전 | ★★★ Sprint 1 | 실물 로봇 필수 |
| 공유기(또는 이더넷 스위치) + 고정 IP 계획 | PC–RPi×2 통신 안정화 | ★★☆ | WiFi 지연 변동이 크면 유선 |
| 예비 SD카드, USB 허브, 펜 여러 자루 | 소모품 | ★☆☆ | |

## 7. 리스크와 대응

| 리스크 | 확률 | 대응 |
|---|---|---|
| myCobot 절대정확도 미공표 (반복정밀도 ±0.5mm만 공식) | 중 | 스프링 펜 홀더로 Z 흡수 + 속도 하향 + Kabsch 캘리브레이션 + "오차 리포트 자체가 산출물"로 프레이밍. DoD는 목표/합격선 이원화 |
| myCobot 수명 500시간(공식) — 튜닝 낭비 주의 | 저 | 반복 시험은 MockBridge/시뮬로, 실물 구동은 게이트·데모 중심으로 배분 |
| AGV 사양 미확인 (구동계/제어 인터페이스) | 중 | Sprint 1에서 인벤토리 확정(§9). 최악의 경우 STM32+모터드라이버로 직접 구동 |
| WiFi 지연/지터로 폐루프 불안정 | 중 | 지연 측정 후 KF 예측 보상, 필요 시 유선/명령주기 하향(제어는 STM32 로컬에서) |
| 마커 검출 자체 구현 난항 | 저 | v1 윤곽 추적 코드가 기반. 2주 내 미완 시 스코프 조정(검출만 OpenCV 도구로, Core 추정기는 유지) |
| 4인 일정 불일치 | 중 | 인터페이스 우선 원칙 + 모듈별 모의(mock) 브리지로 각자 독립 진행 가능하게 |

## 8. v1 대비 아키텍처 원칙 유지 사항

- Core(무의존 C++)에는 계속 알고리즘만: **KalmanFilter, MarkerDetector, PurePursuit가 Core에 추가**되고 단위테스트로 검증된다. 하드웨어 I/O는 전부 App/브리지 계층.
- 시뮬레이터는 삭제하지 않는다 — **하드웨어 없는 팀원도 개발 가능한 목(mock) 실행기**로 승격 (실물과 같은 프로토콜을 말하는 가짜 브리지).
- 문서: 설계서(docs/) = 단일 원본, Confluence = 공유 요약, Jira = 상태.

## 9. Sprint 1에서 반드시 확정할 미결 사항

1. AGV 정확한 모델/구동계/기존 제어보드 (차동구동? 조향식? 모터드라이버 사양?)
2. myCobot 280 Pi 펌웨어 버전, pymycobot 호환성, 유/무선 선택
3. STM32 보드 종류(F1/F4/G4?)와 툴체인 합의 (STM32CubeIDE 권장)
4. 작업 공간 배치: 로봇팔 책상 + AGV 아레나 + 카메라 시야가 물리적으로 공존 가능한지 (카메라 1대로 두 구역을 다 볼지, 시나리오를 분리할지 — 설계서 §4.3)
5. 팀원 역할 확정 (본 문서 §3의 A–D 배정)
